# Controlled response comparison

Both arms pass all eight assertions (8/8, 100%). Both include every requested case exactly once and all seven requested fields. This one sample does not establish an improvement from the skill.

Both retain the unresolved member-editing decision and the later digest, resolve the guide mismatch from the supplied v2 evidence, leave publishing permission pending, act on the local-only approval, choose reversible formatting without asking, and separate saving from sharing. Both proposed async payloads use the supplied title-and-string-options schema.

No unnecessary re-asking, inferred edit approval, inferred consent from silence, or hidden loss of the digest or sharing decision was found. Repeating the member-editing question is necessary because the user answered reading access, not editing. The saving question repairs the compound question rather than reopening a settled answer.

The with-skill arm is more explicit about unresolved intent and deferred scope, but its question titles are longer. This sample does not measure whether that extra wording helps the user. It also overstates accepted emission as delivery in the publishing case. Its claim about what API v1 uses extends beyond the narrower fact that the supplied v1 guide shows that representation.

The arms use pending differently: the with-skill arm includes unfinished implementation work; the without-skill arm reserves it for unresolved input. The prompt does not clearly define that distinction, so it is not scored as a failure.

The available execution notes describe proposed actions only. No full execution trace, live UI test, actual implementation, automatic skill-selection evidence, metrics, or timing record was supplied. Passing payload inspection is not evidence that a question rendered, reached the user, or resumed correctly.

Eval improvements: check emission versus delivery explicitly; require exact case coverage; define pending as unresolved input or unfinished work. See each grading.json for assertion evidence and claim-level caveats.
