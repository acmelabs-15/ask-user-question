{
  "name": "ask-user-question",
  "version": "0.2.0",
  "private": true,
  "description": "Question-and-reply skill with verified host references and independent behavior evaluations.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "changeset": "changeset",
    "version": "changeset version && bun run sync-version",
    "sync-version": "bun run scripts/sync-version.ts",
    "check": "bun run scripts/validate-skill.ts && bun run typecheck && bun run sync-version --check && intent validate"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^1.0.1",
    "@changesets/cli": "^3.0.2",
    "@tanstack/intent": "0.4.0",
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0",
    "yaml": "2.9.0"
  },
  "keywords": [
    "agent-skill",
    "user-questions",
    "tanstack-intent"
  ]
}
