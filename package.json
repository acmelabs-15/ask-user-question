{
  "name": "ask-user-question-evals",
  "private": true,
  "description": "Dev-only manifest. Carries the type packages `make typecheck` needs; the plugin itself ships no dependencies and this file is not part of the artifact.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "tiktoken": "^1.0.22",
    "typescript": "^5.6.0"
  }
}
