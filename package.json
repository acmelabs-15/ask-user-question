{
  "name": "ask-user-question",
  "version": "0.1.6",
  "private": true,
  "description": "Dev-only manifest. Carries the version Changesets bumps and the type packages `make typecheck` needs; the plugin itself ships no dependencies and this file is not part of the artifact.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "changeset": "changeset",
    "version": "changeset version && bun run sync-version",
    "sync-version": "bun run scripts/sync-version.ts"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^1.0.1",
    "@changesets/cli": "^3.0.2",
    "@types/bun": "^1.3.14",
    "tiktoken": "^1.0.22",
    "typescript": "^5.6.0"
  }
}
